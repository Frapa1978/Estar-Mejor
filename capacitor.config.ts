import { CapacitorConfig } from '@capacitor/cli';

 const config: CapacitorConfig = {
   appId: 'com.estarmejor.app',
   appName: 'Estar-Mejor',
   webDir: 'public',
   bundledWebRuntime: false
 };

 export default config;